apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.8
version = '1.0'

//添加maven仓库
repositories {
    mavenCentral()
}

dependencies {
    // Junit
    testCompile group: 'junit', name: 'junit', version: '4.11'

    // Logger
    compile 'log4j:log4j:1.2.16'
}